<%= form_for(@endorsement) do |f| %>
  <% if @endorsement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@endorsement.errors.count, "error") %> prohibited this endorsement from being saved:</h2>

      <ul>
      <% @endorsement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
    <%= f.label :user_id_endorser %><br>
	<%= f.collection_select(:user_id_endorser, @managers, :id, :email, {prompt: "Select Endorser"}, {class: "form-control"}) %>
  </div>
  
  <div class="form-group">
    <%= f.label :user_skill_assignments_id %><br>
	<%= f.collection_select(:user_skill_assignments_id, @skillassignments, :id, :skill_with_level, {prompt: "Select User Skill Assignment"}, {class: "form-control"}) %>
  </div>
  
  <div class="actions">
    <%= f.submit "Save", :class => 'btn btn-primary' %>
  </div>
<% end %>
