<%= form_for(@prerequisite) do |f| %>
  <% if @prerequisite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prerequisite.errors.count, "error") %> prohibited this prerequisite from being saved:</h2>

      <ul>
      <% @prerequisite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
  <div class="form-group">
    <%= f.label :prerequisite_skill_id %><br>
	<%= f.collection_select(:prerequisite_skill_id, Skill.all, :id, :name, {prompt: "Select Prerequisite"}, {class: "form-control"}) %>
  </div>
  
   <div class="form-group">
    <%= f.label :skill_id %><br>
	<%= f.collection_select(:skill_id, Skill.all, :id, :name, {prompt: "Select Skill"}, {class: "form-control"}) %>
  </div>
  
  <div class="actions">
    <%= f.submit "Save", :class => 'btn btn-primary' %>
  </div>
<% end %>
